[project]
name = "nchu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "selenium>=4.27.1",
    "webdriver-manager>=4.0.2",
]
